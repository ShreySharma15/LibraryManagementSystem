# Library Management System (Java + MySQL)

A simple console-based library management system built in Java using JDBC and MySQL.

### Features
- Add new books  
- View all books  
- Issue and return books  
- MySQL backend integration  

### Database Setup
```sql
CREATE DATABASE librarydb;
USE librarydb;
CREATE TABLE books (...);
CREATE TABLE issued_books (...);
